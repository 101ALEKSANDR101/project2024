import { profileActions, profileReducer } from "./model/slice/profileSlice";
import type { ProfileSchema, Profile } from "./model/types/profileSchema";


export { ProfileSchema, Profile, profileReducer, profileActions }